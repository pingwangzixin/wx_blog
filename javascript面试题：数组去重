题目：编写一个数组去重的方法

答案：

        var aa=[1,3,5,4,3,3,1,4]
        function arr(arr) {
          var result=[]
          for(var i=0; i<arr.length; i++){
            if(result.indexOf(arr[i])==-1){
              result.push(arr[i])
            }
          }
          console.log(result)
        }
        arr(aa)
